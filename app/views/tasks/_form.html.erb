<!-- Form partial -->
<%= form_with(model: task, class: "form-container") do |form| %>
  <div class="margin-bottom">
    <%= form.text_field :description, placeholder: "Add new task", class: "input-field" %>
    <%= form.submit "Save", class: "btn btn-primary" %>
  </div>
<% end %>